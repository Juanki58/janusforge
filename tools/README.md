# Tools (local)

Coloca aquí `vina.exe` (Windows) desde [AutoDock Vina releases](https://github.com/ccsb-scripps/AutoDock-Vina/releases).

Si no está, `resolve_vina` también busca `../molforge/tools/vina.exe`. Configurable en `configs/cb1_cb2.yaml` → `docking.vina_binary`.
